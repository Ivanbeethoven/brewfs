[package]
name = "brewfs-stats"
version = "0.1.0"
edition = "2024"
description = "Real-time performance stats viewer for BrewFS (like juicefs stats)"

[dependencies]
clap = { workspace = true, features = ["derive"] }
crossterm = "0.28"
